Follow [`AGENTS.md`](../AGENTS.md) in the repository root.

- If the user wants OpenUsage installed on their Linux machine, use the
  **Install this for the user** section. Run `./install.sh`. Never ask them
  to paste tokens.
- If they want code changes, use **Work on this repo**. Offline tests:
  `python3 -m unittest discover -s tests`.
